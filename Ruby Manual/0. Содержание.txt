Содержание:

I.   Комманды действий
      # алфавитный порядок
      # порядок, по связанности

II. Переменные, строки, символы указатели, и пр.
      # переменные
      # указатели, символы (symbol)
      # работа с текстом
      # работа с фаилами

III. Работа с цифрами (instance)
      # 

IV.  Циклы, функции, классы, модули и т.д.
      # циклы
      # бесконечные циклы
      # функции
      # классы, модули
      # сравнения (end)

V.   Массивы (Array)
      # определения
      # поиск по массиву
      # работа с данными массива (добавление)
      # удаление и очистка массива
      # сотировка массива
      # работа с несколькими массивами и т.д.

VI.  Hash (Dictionary, Hashtable, ассоциативные массивы)
      # пример создания и прочих вариаций
      # примеры поиска и условий


irb                                         - консоль
rails c                                     - консоль
.map!                                       - восклицательный знак, обозначает произвести действие над объектом и сохранить изменения!

P.S. в конце если стоит !, означает что действие над объектом произведется с СОХРАНЕНИЕМ!!
************************************************************************************************************
************************************************************************************************************